name: ruff
on:
  pull_request:
    branches: 
      - main

jobs:
  setup:
    uses: ./.github/workflows/base.yml
  linting:
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - name: Linter
      run: uvx ruff check
    - name: Formatter
      run: uvx ruff format --check